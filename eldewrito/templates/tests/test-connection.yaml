apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eldewrito.fullname" . }}-test-connection"
  labels:
{{ include "eldewrito.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "eldewrito.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
